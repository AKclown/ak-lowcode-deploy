{
  "compilerOptions": {
    "baseUrl": ".",
    "rootDir": ".",
    // "outDir": "dist",
    "noEmit": true
  },
  "references": [
    {
      "path": "core/cli"
    }
  ],
  // "include": ["src/**/*"],
  "exclude": ["node_module", "scripts", "doc"],
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "ak"
}
