{
  "compilerOptions": {
    "baseUrl": ".",
    "rootDir": ".",
    // "outDir": "dist",
    "noEmit": true
  },
  "references": [
    {
      "path": "core/cli"
    }
  ],
  // "include": ["src/**/*"],
  "exclude": ["node_module", "scripts"],
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "ak"
}
